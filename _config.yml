remote_theme: themefisher/kross-jekyll

permalink: pretty
plugins:
  - jekyll-sitemap
compress_html:
  clippings: all
  comments: all
  startings: [html, head, body]

defaults:
  - scope:
      path: '_pages'
    values:
      permalink: /:basename